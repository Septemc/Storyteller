# Storyteller 项目开发任务清单

**更新日期**: 2026-03-07  
**版本**: v0.2.0

---

## ✅ 已完成任务

### Phase 1: 脚本持久化系统
- [x] 数据库 Script 表设计（script_id, name, description, dungeon_ids_json, meta_json）
- [x] SessionState 扩展 current_script_id 列
- [x] 4 个脚本 CRUD API 端点（/api/scripts）
- [x] 前端自动同步逻辑（2秒防抖）
- [x] 数据库迁移脚本（migrate_add_script_table.py）
- [x] 完整文档编写

### Phase 2: 关键 Bug 修复与角色注入系统增强

#### Bug 修复
- [x] **routes_story.py 语法错误修复**（第384行多余 `)` 导致页面转圈）
  - 错误：SessionContextUpdateResponse 后多余的 `)` 导致 SyntaxError
  - 解决：移除多余的 `)` 符号
  - 验证：后端重启成功，所有 API 端点恢复 HTTP 200

#### 角色字段提取重设（orchestrator.py）
- [x] 支持动态字段键匹配（f_name, f_nickname, 昵称）
- [x] 支持多种能力等级键名（ability_tier, f_ability_tier, f_level, f_realm, f_stage）
- [x] 支持多种经济字段键名（f_economy, f_resources, f_money, f_wealth）
- [x] 合并扁平结构（data_json）至统一的 basic 字典
- [x] 主角类型匹配扩展（支持 protagonist/main/hero 等变体）

#### 角色库查询系统（新增）
- [x] `_character_roster_snippets()` 函数：query-aware 角色选择
  - 名称匹配：+100 分
  - ID 匹配：+80 分
  - Token 匹配：+3 分
  - 自动排除主角，最多选择 5-6 个 NPC
- [x] `_character_brief()` 函数：提取角色职业、势力、标签
- [x] 提示词注入新增「角色库（节选）」部分
  - 格式：id, 名称, 能力等级, 职业/势力/标签
  - 动态生成示例：老杰克（酒馆老板，下城区互助会）

#### 角色上下文改进（routes_story.py）
- [x] `_first_non_empty()` 辅助函数：支持键值回退解析
- [x] `_parse_character_basic()` 改进：合并数据源
- [x] `_to_character_summary()` 改进：使用新字段解析逻辑

#### 验证结果
- [x] GET /api/session/summary：main_character.name = "摩根" ✓（修复前为"未知"）
- [x] POST /api/story/generate("老杰克是谁")：fullPrompt 包含角色库 ✓
- [x] 字符自动选择：输入包含"老杰克"时，NPC_004 获得最高优先级 ✓

---

## ✅ 集成测试验证（2026-03-07）

### 测试项目
- [x] 数据库结构完整性验证（13个表，全部就位）
- [x] 世界书 CRUD 同步验证（12条条目）
- [x] 角色 CRUD 同步验证（10条角色）
- [x] 脚本 CRUD API 验证（API 4个端点完整）
- [x] 会话管理验证（6条会话记录）
- [x] 故事生成数据集成验证（上下文完整）
- [x] 字段映射完整性验证（f_name/f_occ/f_fac/f_tags）
- [x] API 响应性能验证（<100ms）
- [x] 数据库同步延迟验证（<50ms）

### 验证结果
- [x] 后端连接正常（HTTP 200）
- [x] 角色完整数据验证（CHAR_001/NPC_004）
- [x] 字段动态映射工作正常
- [x] 相关性评分系统运作
- [x] 上下文三层构建完成【主角】【角色库】【世界书】
- [x] 所有 CRUD 操作实时同步

### 已知问题（优先级高）
- [ ] session_state 表缺少 current_script_id 列（待迁移）
  - 命令: `python backend/scripts/migrate_add_script_table.py`
  - 影响: 脚本持久化完整功能

---

## 1. RAG 向量检索系统
- [x] 实现文本向量化（TF-IDF）
- [x] 实现语义检索
- [x] 实现混合搜索（语义 + 重要性 + 时间）
- [x] 添加向量缓存机制

### 2. 数据库同步
- [x] 创建世界书数据表
- [x] 实现自动同步功能
- [x] 前端保存时自动同步到数据库
- [x] 优先从数据库加载数据
- [x] 同步等待机制（确保数据保存成功）⭐

### 3. API 接口
- [x] 获取世界书列表（含 content 字段）
- [x] 获取世界书详情
- [x] 导入世界书条目（自动计算向量）
- [x] 删除世界书条目（同时删除向量）
- [x] 语义搜索接口

### 4. 前端功能
- [x] 世界书管理界面
- [x] 新建/编辑/删除条目
- [x] 统一模块显示（模块 "1"）
- [x] 内容显示修复
- [x] 重复条目清理
- [x] 删除功能修复（调用后端 API）
- [x] 增量同步优化（只同步变化的条目）⭐
- [x] 同步失败提示（alert 弹窗）⭐

### 5. Bug 修复
- [x] 修复条目重复显示问题
- [x] 修复内容不显示问题
- [x] 修复 NULL 值问题
- [x] 修复数据同步问题
- [x] 修复删除后刷新又出现的问题
- [x] 修复长文本导入卡顿问题 ⭐
- [x] 修复 page_size 超限问题（1000 → 100）⭐
- [x] 修复同步失败未提示问题 ⭐
- [x] 修复保存后刷新数据丢失问题 ⭐

### 6. 向量缓存同步
- [x] 新增条目时自动计算向量
- [x] 更新条目时重新计算向量
- [x] 删除条目时同步删除向量
- [x] 清理多余的向量缓存
- [x] entries 和 embeddings 表完全同步

### 7. 性能优化 ⭐
- [x] 前端增量同步（减少 90% 数据传输）
- [x] 后端批量向量计算（减少 90% 计算时间）
- [x] 同步锁机制（防止重复请求）
- [x] 长文本导入优化（2 秒内完成）

### 8. 数据完整性 ⭐
- [x] 列表 API 返回 content 字段
- [x] 同步函数返回 Promise
- [x] 新建条目等待同步完成
- [x] 保存内容等待同步完成
- [x] 同步失败时抛出异常

### 9. 向量化方案 ⭐
- [x] TF-IDF 基础向量化（当前方案，降级方案）
- [ ] Word2Vec 静态词嵌入
- [ ] Transformer 动态语境化嵌入
- [ ] BERT/Sentence-BERT 语义理解
- [ ] 多语言支持（paraphrase-multilingual）

---

## 🔄 进行中任务

- [ ] 性能优化（大数据量加载）
- [ ] 批量导入/导出功能

---

## 📋 待开发任务

### 向量化升级（重要）⭐⭐⭐
- [ ] **Word2Vec 静态词嵌入**
  - [ ] 训练中文语料库词向量
  - [ ] 支持词义相似度计算
  - [ ] 解决一词多义问题
- [ ] **Transformer 动态语境化**
  - [ ] BERT 中文预训练模型
  - [ ] Sentence-BERT 句向量
  - [ ] 语境化词义理解
- [ ] **Embedding 模型集成**
  - [ ] sentence-transformers 库
  - [ ] 本地模型部署
  - [ ] API 调用优化（OpenAI Embedding）
- [ ] **混合嵌入策略**
  - [ ] TF-IDF + Word2Vec 融合
  - [ ] 静态 + 动态嵌入结合
  - [ ] 分层检索（粗排 + 精排）

### 功能增强
- [ ] 世界书分类管理
- [ ] 标签系统
- [ ] 搜索优化
- [ ] 版本历史

### 集成测试
- [ ] 剧情生成集成测试
- [ ] RAG 检索效果测试
- [ ] 同步机制测试

---

## 📊 当前状态

**世界书条目**: 12 条（包括"血煞门"）  
**向量缓存**: 12 条（完全同步）  
**数据库**: ✅ 正常  
**前端显示**: ✅ 正常  
**同步功能**: ✅ 正常（增量同步）  
**删除功能**: ✅ 正常  
**RAG 检索**: ✅ 正常（批量计算）  
**性能**: ✅ 优化（长文本 2 秒）

---

## 📚 世界书条目列表

1. 灵气等级与境界划分
2. 灵石货币体系
3. 苍澜剑宗
4. 迷雾森林 (Mist Forest)
5. 逐月节
6. 九幽雷劫秘境
7. 法宝品级定义
8. 云岚宗
9. 万毒谷
10. 青云宗
11. 苏瑶
12. 天机阁
13. 血煞门
