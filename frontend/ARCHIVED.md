# Archived Frontend

The legacy static frontend has been archived to `archive/frontend_legacy`.

The active application frontend now lives under `frontend_vue/src`.

Do not add new runtime code under this directory.
